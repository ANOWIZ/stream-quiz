# npm run dev creates .env with random credentials when it does not exist.
# These placeholders are for manual setup only.
HOST_PASSWORD=
PLAYER_PASSWORD=
SESSION_SECRET=
DATABASE_URL=file:../data/quiz.db
PORT=3001
NODE_ENV=development
TRUST_PROXY=0
